# Colony Protocol Manual

## Section 1
This section describes procedure number 1. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 1 is active.

## Section 2
This section describes procedure number 2. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 2 is active.

## Section 3
This section describes procedure number 3. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 3 is active.

## Section 4
This section describes procedure number 4. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 4 is active.

## Section 5
This section describes procedure number 5. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 5 is active.

## Section 6
This section describes procedure number 6. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 6 is active.

## Section 7
This section describes procedure number 7. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 7 is active.

## Section 8
This section describes procedure number 8. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 8 is active.

## Section 9
This section describes procedure number 9. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 9 is active.

## Section 10
This section describes procedure number 10. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 10 is active.

## Section 11
This section describes procedure number 11. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 11 is active.

## Section 12
This section describes procedure number 12. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 12 is active.

## Section 13
This section describes procedure number 13. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 13 is active.

## Section 14
This section describes procedure number 14. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 14 is active.

## Section 15
This section describes procedure number 15. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 15 is active.

## Section 16
This section describes procedure number 16. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 16 is active.

## Section 17
This section describes procedure number 17. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 17 is active.

## Section 18
This section describes procedure number 18. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 18 is active.

## Section 19
This section describes procedure number 19. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 19 is active.

## Section 20
This section describes procedure number 20. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 20 is active.

## Section 21
This section describes procedure number 21. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 21 is active.

## Section 22
This section describes procedure number 22. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 22 is active.

## Section 23
This section describes procedure number 23. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 23 is active.

## Section 24
This section describes procedure number 24. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 24 is active.

## Section 25
This section describes procedure number 25. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 25 is active.

## Section 26
This section describes procedure number 26. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 26 is active.

## Section 27
This section describes procedure number 27. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 27 is active.

## Section 28
This section describes procedure number 28. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 28 is active.

## Section 29
This section describes procedure number 29. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 29 is active.

## Section 30
This section describes procedure number 30. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 30 is active.

## Section 31
This section describes procedure number 31. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 31 is active.

## Section 32
This section describes procedure number 32. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 32 is active.

## Section 33
This section describes procedure number 33. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 33 is active.

## Section 34
This section describes procedure number 34. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 34 is active.

## Section 35
This section describes procedure number 35. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 35 is active.

## Section 36
This section describes procedure number 36. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 36 is active.

## Section 37
This section describes procedure number 37. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 37 is active.

## Section 38
This section describes procedure number 38. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 38 is active.

## Section 39
This section describes procedure number 39. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 39 is active.

## Section 40
This section describes procedure number 40. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 40 is active.

## Section 41
This section describes procedure number 41. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 41 is active.

## Section 42
This section describes procedure number 42. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 42 is active.

## Section 43
This section describes procedure number 43. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 43 is active.

## Section 44
This section describes procedure number 44. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 44 is active.

## Section 45
This section describes procedure number 45. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 45 is active.

## Section 46
This section describes procedure number 46. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 46 is active.

## Section 47
This section describes procedure number 47. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 47 is active.

## Section 48
This section describes procedure number 48. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 48 is active.

## Section 49
This section describes procedure number 49. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 49 is active.

## Section 50
This section describes procedure number 50. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 50 is active.

## Section 51
This section describes procedure number 51. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 51 is active.

## Section 52
This section describes procedure number 52. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 52 is active.

## Section 53
This section describes procedure number 53. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 53 is active.

## Section 54
This section describes procedure number 54. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 54 is active.

## Section 55
This section describes procedure number 55. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 55 is active.

## Section 56
This section describes procedure number 56. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 56 is active.

## Section 57
This section describes procedure number 57. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 57 is active.

## Section 58
This section describes procedure number 58. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 58 is active.

## Section 59
This section describes procedure number 59. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 59 is active.

## Section 60
This section describes procedure number 60. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 60 is active.

## Section 61
This section describes procedure number 61. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 61 is active.

## Section 62
This section describes procedure number 62. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 62 is active.

## Section 63
This section describes procedure number 63. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 63 is active.

## Section 64
This section describes procedure number 64. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 64 is active.

## Section 65
This section describes procedure number 65. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 65 is active.

## Section 66
This section describes procedure number 66. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 66 is active.

## Section 67
This section describes procedure number 67. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 67 is active.

## Section 68
This section describes procedure number 68. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 68 is active.

## Section 69
This section describes procedure number 69. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 69 is active.

## Section 70
This section describes procedure number 70. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 70 is active.

## Section 71
This section describes procedure number 71. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 71 is active.

## Section 72
This section describes procedure number 72. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 72 is active.

## Section 73
This section describes procedure number 73. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 73 is active.

## Section 74
This section describes procedure number 74. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 74 is active.

## Section 75
This section describes procedure number 75. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 75 is active.

## Section 76
This section describes procedure number 76. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 76 is active.

## Section 77
This section describes procedure number 77. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 77 is active.

## Section 78
This section describes procedure number 78. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 78 is active.

## Section 79
This section describes procedure number 79. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 79 is active.

## Section 80
This section describes procedure number 80. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 80 is active.

## Section 81
This section describes procedure number 81. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 81 is active.

## Section 82
This section describes procedure number 82. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 82 is active.

## Section 83
This section describes procedure number 83. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 83 is active.

## Section 84
This section describes procedure number 84. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 84 is active.

## Section 85
This section describes procedure number 85. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 85 is active.

## Section 86
This section describes procedure number 86. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 86 is active.

## Section 87
This section describes procedure number 87. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 87 is active.

## Section 88
This section describes procedure number 88. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 88 is active.

## Section 89
This section describes procedure number 89. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 89 is active.

## Section 90
This section describes procedure number 90. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 90 is active.

## Section 91
This section describes procedure number 91. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 91 is active.

## Section 92
This section describes procedure number 92. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 92 is active.

## Section 93
This section describes procedure number 93. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 93 is active.

## Section 94
This section describes procedure number 94. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 94 is active.

## Section 95
This section describes procedure number 95. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 95 is active.

## Section 96
This section describes procedure number 96. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 96 is active.

## Section 97
This section describes procedure number 97. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 97 is active.

## Section 98
This section describes procedure number 98. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 98 is active.

## Section 99
This section describes procedure number 99. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 99 is active.

## Section 100
This section describes procedure number 100. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 100 is active.

## Section 101
This section describes procedure number 101. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 101 is active.

## Section 102
This section describes procedure number 102. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 102 is active.

## Section 103
This section describes procedure number 103. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 103 is active.

## Section 104
This section describes procedure number 104. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 104 is active.

## Section 105
This section describes procedure number 105. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 105 is active.

## Section 106
This section describes procedure number 106. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 106 is active.

## Section 107
This section describes procedure number 107. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 107 is active.

## Section 108
This section describes procedure number 108. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 108 is active.

## Section 109
This section describes procedure number 109. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 109 is active.

## Section 110
This section describes procedure number 110. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 110 is active.

## Section 111
This section describes procedure number 111. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 111 is active.

## Section 112
This section describes procedure number 112. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 112 is active.

## Section 113
This section describes procedure number 113. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 113 is active.

## Section 114
This section describes procedure number 114. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 114 is active.

## Section 115
This section describes procedure number 115. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 115 is active.

## Section 116
This section describes procedure number 116. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 116 is active.

## Section 117
This section describes procedure number 117. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 117 is active.

## Section 118
This section describes procedure number 118. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 118 is active.

## Section 119
This section describes procedure number 119. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 119 is active.

## Section 120
This section describes procedure number 120. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 120 is active.

## Section 121
This section describes procedure number 121. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 121 is active.

## Section 122
This section describes procedure number 122. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 122 is active.

## Section 123
This section describes procedure number 123. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 123 is active.

## Section 124
This section describes procedure number 124. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 124 is active.

## Section 125
This section describes procedure number 125. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 125 is active.

## Section 126
This section describes procedure number 126. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 126 is active.

## Section 127
This section describes procedure number 127. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 127 is active.

## Section 128
This section describes procedure number 128. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 128 is active.

## Section 129
This section describes procedure number 129. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 129 is active.

## Section 130
This section describes procedure number 130. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 130 is active.

## Section 131
This section describes procedure number 131. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 131 is active.

## Section 132
This section describes procedure number 132. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 132 is active.

## Section 133
This section describes procedure number 133. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 133 is active.

## Section 134
This section describes procedure number 134. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 134 is active.

## Section 135
This section describes procedure number 135. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 135 is active.

## Section 136
This section describes procedure number 136. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 136 is active.

## Section 137
This section describes procedure number 137. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 137 is active.

## Section 138
This section describes procedure number 138. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 138 is active.

## Section 139
This section describes procedure number 139. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 139 is active.

## Section 140
This section describes procedure number 140. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 140 is active.

## Section 141
This section describes procedure number 141. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 141 is active.

## Section 142
This section describes procedure number 142. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 142 is active.

## Section 143
This section describes procedure number 143. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 143 is active.

## Section 144
This section describes procedure number 144. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 144 is active.

## Section 145
This section describes procedure number 145. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 145 is active.

## Section 146
This section describes procedure number 146. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 146 is active.

## Section 147
This section describes procedure number 147. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 147 is active.

## Section 148
This section describes procedure number 148. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 148 is active.

## Section 149
This section describes procedure number 149. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 149 is active.

## Section 150
This section describes procedure number 150. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 150 is active.

## Section 151
This section describes procedure number 151. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 151 is active.

## Section 152
This section describes procedure number 152. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 152 is active.

## Section 153
This section describes procedure number 153. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 153 is active.

## Section 154
This section describes procedure number 154. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 154 is active.

## Section 155
This section describes procedure number 155. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 155 is active.

## Section 156
This section describes procedure number 156. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 156 is active.

## Section 157
This section describes procedure number 157. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 157 is active.

## Section 158
This section describes procedure number 158. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 158 is active.

## Section 159
This section describes procedure number 159. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 159 is active.

## Section 160
This section describes procedure number 160. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 160 is active.

## Section 161
This section describes procedure number 161. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 161 is active.

## Section 162
This section describes procedure number 162. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 162 is active.

## Section 163
This section describes procedure number 163. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 163 is active.

## Section 164
This section describes procedure number 164. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 164 is active.

## Section 165
This section describes procedure number 165. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 165 is active.

## Section 166
This section describes procedure number 166. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 166 is active.

## Section 167
This section describes procedure number 167. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 167 is active.

## Section 168
This section describes procedure number 168. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 168 is active.

## Section 169
This section describes procedure number 169. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 169 is active.

## Section 170
This section describes procedure number 170. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 170 is active.

## Section 171
This section describes procedure number 171. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 171 is active.

## Section 172
This section describes procedure number 172. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 172 is active.

## Section 173
This section describes procedure number 173. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 173 is active.

## Section 174
This section describes procedure number 174. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 174 is active.

## Section 175
This section describes procedure number 175. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 175 is active.

## Section 176
This section describes procedure number 176. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 176 is active.

## Section 177
This section describes procedure number 177. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 4. The medical override code is M-77 when section 177 is active.

## Section 178
This section describes procedure number 178. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 9. The medical override code is M-77 when section 178 is active.

## Section 179
This section describes procedure number 179. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 3. The default communication channel is 9. The medical override code is M-77 when section 179 is active.

## Section 180
This section describes procedure number 180. The backup power threshold is 40 percent. The correct airlock for cargo is airlock 2. The default communication channel is 4. The medical override code is M-77 when section 180 is active.
